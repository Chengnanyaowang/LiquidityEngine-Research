# Synthetic examples

Every file in this directory is synthetic. The fixture demonstrates the shape of an evidence bundle and deliberately leads to a `wait` research state because the synthetic structure remains unresolved.

It is not derived from a live market, customer account, private replay, or production strategy output.
